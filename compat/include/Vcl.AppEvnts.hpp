/// C++Builder の Vcl.AppEvnts.hpp に対応する互換ヘッダへの転送
///
/// `TApplicationEvents` (Application のイベントをフォームに配る部品) のユニット。
/// src では `src/ColPicker.h:29` と `src/FuncDlg.h` がメンバとして保持している。
/// 型そのものは compat/gui_stubs.h に宣言のみで置く。
#ifndef NYANFI_FWD_VCL_APPEVNTS_HPP
#define NYANFI_FWD_VCL_APPEVNTS_HPP

#include "compat/gui_stubs.h"

#endif
